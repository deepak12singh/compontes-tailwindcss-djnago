

---

# 📘 Django + TailwindCSS Template System

## (Complete English Documentation)

This documentation explains a reusable **Django + TailwindCSS template system** where:

* TailwindCSS is packaged as a reusable Django app
* Users can enable Tailwind by simply copying folders
* Users can create their **own `base.html`**
* Clear separation between `core.html` and `base.html`
* Both **development** and **production** workflows are defined

---

## 📂 Overall Architecture

```text
My_Project/
│
├── My_Project/                # Django project settings
├── Applection/                # Your Django app
│   └── templates/
│       └── Applection/
│           └── base.html      # (User custom base template)
│
├── tailwindcss/               # Tailwind Django App
│   ├── templates/
│   │   └── tailwindcss/
│   │       ├── core.html
│   │       ├── base.html
│   │       └── includes/
│   │           ├── header.html
│   │           ├── footer.html
│   │           └── scripts.html
│   │
│   └── static/
│       └── tailwindcss/
│           └── css/
│               ├── main.css
│               └── style.css
│
├── z_dev/                     # Tailwind build tool (Node.js)
└── manage.py
```

---

# 1️⃣ `tailwindcss/core.html`

## (Root / Skeleton Template)

### 🔹 What is this file?

`core.html` is the **foundation of the entire template system**.

* It only defines structure
* No UI design
* No header or footer
* Only reusable Django blocks (hooks)

👉 **Rule:**

> This file is **not used directly for pages**
> It is meant only to be extended

---

## 🧱 Purpose of `core.html`

* Defines `<html>`, `<head>`, and `<body>`
* Loads Tailwind `main.css`
* Provides blocks for SEO, CSS, JS, layout, header, footer

---

## 🔹 HTML-Level Blocks

```django
<html lang="{% block html_lang %}en{% endblock %}"
      class="{% block html_class %}{% endblock %}">
```

### Explanation:

* **`html_lang`**
  → Website language
  → Example: `en`, `hi`, `en-IN`

* **`html_class`**
  → Classes on `<html>` tag
  → Useful for dark mode or global themes

---

## 🔹 `<head>` Section

```django
{% block head %}
```

Wrapper for the entire `<head>` section.

### Meta Block

```django
{% block meta %}
```

* Charset
* Viewport
* Base SEO tags

---

### Title Block

```django
{% block title %}{% endblock %}
```

* Browser tab title
* Overridden per page

---

### Tailwind CSS (Required)

```django
<link rel="stylesheet" href="{% static 'tailwindcss/css/main.css' %}">
```

* Generated by `npm run dev` / `npm run build`
* **This is the production CSS file**

---

### Extra Head Blocks

| Block   | Purpose                          |
| ------- | -------------------------------- |
| `seo`   | Meta description, OpenGraph tags |
| `css`   | Additional CSS files             |
| `style` | Inline CSS                       |

---

## 🔹 `<body>` Section

```django
<body class="{% block body_class %}{% endblock %}">
```

* **`body_class`**
  → Tailwind classes for `<body>`
  → Example: `bg-gray-100 text-gray-900`

---

### Body Layout Blocks

```django
{% block body %}
    {% block header %}{% endblock %}
    {% block layout %}{% endblock %}
    {% block footer %}{% endblock %}
{% endblock %}
```

* `header` → Navbar / Top section
* `layout` → Main content area
* `footer` → Footer section

---

## 🔹 JavaScript Blocks

| Block    | Usage                    |
| -------- | ------------------------ |
| `js`     | JavaScript file includes |
| `script` | Inline JavaScript        |

---

## ✅ `core.html` Summary

* Acts as a framework
* Contains no UI decisions
* Provides maximum flexibility
* Best for advanced customization

---

# 2️⃣ `tailwindcss/base.html`

## (Default Ready-to-Use Layout)

### 🔹 What is this file?

`base.html` is a **ready-made layout** built on top of `core.html`.

👉 This is the file **most users should extend**

---

## 🧱 Purpose of `base.html`

* Includes header and footer
* Sets default SEO
* Defines main content area
* Provides quick inheritance

---

## 🔹 Extending Core

```django
{% extends "tailwindcss/core.html" %}
```

Meaning:

* HTML structure comes from `core.html`
* This file fills in the blocks

---

## 🔹 Title

```django
{% block title %}My Site{% endblock %}
```

* Default site title
* Can be overridden per page

---

## 🔹 SEO Block

```django
{% block seo %}
<meta name="description" content="{% block meta_description %}Default website description{% endblock %}">
<link rel="icon" href="{% static 'tailwindcss/images/favicon.ico' %}">
{% endblock %}
```

* Default meta description
* Nested `meta_description` block
* Favicon included

---

## 🔹 Extra CSS

```django
{% block css %}
<link rel="stylesheet" href="{% static 'tailwindcss/css/style.css' %}">
{% endblock %}
```

* Custom CSS on top of Tailwind

---

## 🔹 Header Include

```django
{% include "tailwindcss/includes/header.html" %}
```

* Shared header
* Single place to update navigation

---

## 🔹 Main Content Area

```django
{% block layout %}
<main id="content">
    {% block content %}{% endblock %}
</main>
{% endblock %}
```

👉 The `content` block is the **most frequently used block**

---

## 🔹 Footer & Scripts

* Footer is globally included
* JavaScript files are centralized

---

## ✅ `base.html` Summary

* Ready layout
* SEO + Tailwind enabled
* Ideal for ~90% of projects

---

# 3️⃣ Creating Your Own `base.html`

Users have **two valid approaches**.

---

## 🥇 Method 1: Extend `core.html`

### (Full Control)

```django
{% extends "tailwindcss/core.html" %}

{% block header %}
<header class="bg-black text-white p-4">My Custom Header</header>
{% endblock %}

{% block layout %}
<main class="p-6">
    {% block content %}{% endblock %}
</main>
{% endblock %}
```

✔ Use when you need full design freedom
✔ Header/footer are app-specific

---

## 🥈 Method 2: Extend `tailwindcss/base.html`

### (Recommended)

```django
{% extends "tailwindcss/base.html" %}

{% block title %}Dashboard{% endblock %}
{% block body_class %}bg-gray-100{% endblock %}

{% block content %}
<h1 class="text-2xl font-bold">Welcome</h1>
{% endblock %}
```

✔ Faster development
✔ Cleaner structure
✔ Easier maintenance

---

## 🔍 Comparison

| Feature  | core.html        | base.html     |
| -------- | ---------------- | ------------- |
| Control  | Full             | Limited       |
| Speed    | Slower           | Faster        |
| Best for | Advanced layouts | Most projects |

---

# 4️⃣ Development vs Production Workflow

## 🔧 Development

Run in **two separate terminals**:

```bash
npm run dev
```

```bash
python manage.py runserver
```

* Tailwind runs in watch mode
* CSS updates automatically

---

## 🏗️ Production

```bash
npm run build
```

* Generates optimized CSS
* After this, `z_dev/` can be safely deleted

---

# 5️⃣ Git / Commit Rules

### ✅ Commit These

* `tailwindcss/core.html`
* `tailwindcss/base.html`
* `tailwindcss/includes/*`
* `tailwindcss/css/main.css`

### ❌ Do NOT Commit

* `z_dev/`
* `node_modules/`

---

# 🏁 Final Conclusion

* ✔ Users can create their own `base.html`
* ✔ They can extend **either** `core.html` or `base.html`
* ✔ Both approaches are valid
* ✔ Architecture is clean and scalable

### Design Philosophy

> `core.html` → framework
> `base.html` → default layout
> `app base.html` → customization layer

---


